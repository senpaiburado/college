<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<title>Добавити новину</title>
	<script src="/pages/ckeditor/ckeditor.js"></script>
</head>
<body>
	<form enctype="multipart/form-data" method="post" action="/admin/addnew">
		<textarea name="content" id="editor">
			<% if (typeof lastText !== "undefined") { %>
				<%= lastText %>
			<% } %>
		</textarea>
		<% if (typeof lastText == "undefined") { %>
			<p><label>Зображення</label><input type="file" name="image" accept="*.png, *.jpg, *.jpeg, *.bmp, *.gif"></p>
		<% } else { %>
			<input type="hidden" name="id" value="<%=id%>">
		<% } %>
		<input type="submit" value="Добавити новину">
	</form>

	<script type="text/javascript">
        CKEDITOR.replace("editor", {
        	width: "780px",
        	margin: "0 auto",
        	minHeight: "170px",
        	removePlugins: "image, document, tableselection, tabletools, table, source"
        });
	</script>
</body>
</html>